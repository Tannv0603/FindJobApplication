@using DAL.Entities;
@using WebApp.Models.ViewModel;
@model ProfileViewModel;
@{
    var cityOpt = new List<SelectListItem>();
    foreach (var city in ViewBag.Cities)
    {
        cityOpt.Add(new SelectListItem() { Text = city.CityName });
    }
}


<link rel="stylesheet" href="/web/css/profile.css">
<script type="text/javascript">
    function Imagechanged(){
        console.log(document.getElementById("IsChanged").value);
        document.getElementById("IsChanged").value="changed";
        console.log(document.getElementById("IsChanged").value);
    }
</script>
<!-- Hero Area Start-->
<div class="slider-area ">
    <div class="single-slider section-overly slider-height2 d-flex align-items-center"
         data-background="/web/img/hero/about.jpg">
        <div class="container">
            <div class="row">
                <div class="col-xl-12">
                    <div class="hero-cap text-center">
                        <h2>Profile</h2>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
<div class="job-post-company pt-50 pb-50">
    <div class="container">
        <div class="main-body">
            @using (Html.BeginForm("UpdateProfile", "Employee", FormMethod.Post,new {enctype = "multipart/form-data" }))
            {
                <div class="row">
                    <div class="col-lg-4">
                        <div class="card">
                            <div class="card-body">
                                <div class="d-flex flex-column align-items-center text-center">
                                    <div id="profile-container">
                                        <image id="profileImage" src="@Model.AvatarUrl" />
                                    </div>
                                    <input id="imageUpload" onchange="Imagechanged()" accept="image/gif, image/jpeg, image/png" type="file" name="request.File" placeholder="Photo" />
                                    <input type="hidden" value="@Model.AvatarUrl" name="request.IfChanged" id="IsChanged" />
                                    <div class="mt-3">
                                        <h4>@Model.FullName</h4>
                                    </div>
                                </div>
                                <hr class="my-4">
                                <ul class="list-group list-group-flush">
                                    <li class="list-group-item d-flex justify-content-between align-items-center flex-wrap">
                                        <h6 class="mb-0">
                                            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24"
                                             viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"
                                             stroke-linecap="round" stroke-linejoin="round"
                                             class="feather feather-globe me-2 icon-inline">
                                                <circle cx="12" cy="12" r="10"></circle>
                                                <line x1="2" y1="12" x2="22" y2="12"></line>
                                                <path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z">
                                                </path>
                                            </svg>Email
                                        </h6>
                                        <span class="text-secondary">@Model.Email</span>
                                    </li>
                                </ul>
                            </div>
                        </div>
                    </div>
                    <div class="col-lg-8">
                        <div class="card">
                            <div class="card-body">
                                <div class="row mb-3">
                                    <div class="col-sm-3">
                                        <h6 class="mb-0">Full Name</h6>
                                    </div>
                                    <div class="col-sm-9 text-secondary">
                                        <input type="text" class="form-control" name="request.FullName" value="@Model.FullName">
                                    </div>
                                </div>
                                <div class="row mb-3">
                                    <div class="col-sm-3">
                                        <h6 class="mb-0">Day of Birth</h6>
                                    </div>
                                    <div class="col-sm-9 text-secondary">
                                        <input type="text" class="form-control" name="request.DayOfBirth" value="@Model.DayOfBirth.ToShortDateString()"
                                           onfocus="(this.type='date')" onblur="if(this.value==''){this.type='text'}" placeholder="@Model.DayOfBirth.ToShortDateString()">
                                    </div>
                                </div>
                                <div class="row mb-3">
                                    <div class="col-sm-3">
                                        <h6 class="mb-0">Phone</h6>
                                    </div>
                                    <div style="border: 0px double #ced4da;" class="col-sm-9 text-secondary">
                                        <input type="text" class="form-control" name="request.PhoneNumber" value="@Model.PhoneNumber">
                                    </div>
                                </div>
                                <div class="row mb-3">
                                    <div class="col-sm-3">
                                        <h6 class="mb-0">Address</h6>
                                    </div>
                                    <div class="col-sm-9 text-secondary">
                                        <input type="text" class="form-control" name="request.Address" placeholder="@Model.Address" value="@Model.Address">
                                    </div>
                                </div>
                                <div class="row mb-3">
                                    <div class="col-sm-3">
                                        <h6 class="mb-0">City</h6>
                                    </div>
                                    <div class="col-sm-9 text-secondary">

                                        <select type="text" class="form-control" name="request.CityName" value="@Model.CityName">
                                            <option value="@Model.CityName" selected >@Model.CityName</option>
                                            @foreach (var city in ViewBag.Cities)
                                            {
                                                <option value="@city.CityName">@city.CityName</option>
                                            }
                                        </select>
                                    </div>
                                </div>
                                <div class="row">
                                    <div class="col-sm-3"></div>
                                    <div class="col-sm-9 text-secondary">
                                        <input type="submit" class="btn btn-primary px-4" value="Save Changes">
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            }

        </div>
    </div>
</div>
<script src="/web/js/profile.js"></script>
